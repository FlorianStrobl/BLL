// ASM generation from the AST, can be in an ascii format or in a binary object file
export namespace Compiler {
  export function compile(ast: any, code: string, fileName: string) {
    return "";
  }
}